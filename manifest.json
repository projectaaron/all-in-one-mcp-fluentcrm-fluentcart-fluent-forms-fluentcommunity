{
  "manifest_version": "0.3",
  "name": "fluentmcp",
  "display_name": "All-In-One MCP for Fluent Suite",
  "version": "1.2.1",
  "description": "Full control of FluentCRM, FluentCart, WP Social Ninja, Fluent Forms, and FluentCommunity on your WordPress site: contacts, campaigns, orders, products, reviews, forms, spaces, courses, and more.",
  "long_description": "Gives your AI assistant create/read/update/delete control over FluentCRM (email marketing & CRM), FluentCart (e-commerce), WP Social Ninja (reviews, social feeds, chat widgets), Fluent Forms (forms & submissions) and FluentCommunity (spaces, feeds, chat, courses) by WPManageNinja — 1,290 documented REST endpoints, each as its own individualized tool (crm_contacts_list, cart_orders_refund, social_reviews_list, forms_submissions_list, community_spaces_list, …), with a built-in tool_map index so the assistant finds the right tool in one call. Safe by default: every destructive tool (deletes, refunds, cancels, mass-sends, resets) is annotated and refuses to run without explicit confirmation. One WordPress Application Password runs everything; run the verify_setup tool after installing to check your connection. Prefer a small tool list? Set the Tool Surface option to \"grouped\".",
  "author": {
    "name": "projectaaron",
    "url": "https://upfluent.io"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/projectaaron/all-in-one-mcp-fluentcrm-fluentcart-fluent-forms-fluentcommunity"
  },
  "homepage": "https://upfluent.io",
  "documentation": "https://github.com/projectaaron/all-in-one-mcp-fluentcrm-fluentcart-fluent-forms-fluentcommunity#readme",
  "support": "https://github.com/projectaaron/all-in-one-mcp-fluentcrm-fluentcart-fluent-forms-fluentcommunity/issues",
  "license": "MIT",
  "icon": "icon.png",
  "keywords": [
    "wordpress",
    "fluentcrm",
    "fluentcart",
    "fluent-forms",
    "fluentcommunity",
    "wp-social-ninja",
    "crm",
    "ecommerce",
    "wpmanageninja"
  ],
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/index.js"
      ],
      "env": {
        "FLUENT_SITE_URL": "${user_config.site_url}",
        "FLUENT_API_USERNAME": "${user_config.api_username}",
        "FLUENT_API_PASSWORD": "${user_config.api_password}",
        "FLUENT_TOOL_MODE": "${user_config.tool_mode}"
      }
    }
  },
  "user_config": {
    "site_url": {
      "type": "string",
      "title": "WordPress Site URL",
      "description": "Your WordPress site root, e.g. https://example.com (not the /wp-admin URL).",
      "required": true
    },
    "api_username": {
      "type": "string",
      "title": "WordPress Username",
      "description": "The WordPress admin user the server acts as (needs manager permissions for the Fluent products you use).",
      "required": true
    },
    "api_password": {
      "type": "string",
      "title": "Application Password",
      "description": "Create under WP Admin → Users → your user → Application Passwords → Add New. One password runs every product's tools.",
      "sensitive": true,
      "required": true
    },
    "tool_mode": {
      "type": "string",
      "title": "Tool Surface",
      "description": "individual (default): one tool per operation, ~1,200 small tools plus a tool_map index. grouped: the legacy ~70-tool surface (one tool per area, an action parameter picks the operation) for clients that struggle with large tool lists.",
      "default": "individual",
      "required": false
    }
  },
  "tools_generated": true,
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=20.6.0"
    }
  },
  "tools": [
    {
      "name": "tool_map",
      "description": "This map — overview, per-area drill-down, keyword search."
    },
    {
      "name": "verify_setup",
      "description": "Check credentials, connectivity, and plugin presence per product."
    },
    {
      "name": "support_report",
      "description": "Redacted diagnostic report (version, connection checks, recent errors) to paste into a support request or GitHub issue."
    }
  ]
}
