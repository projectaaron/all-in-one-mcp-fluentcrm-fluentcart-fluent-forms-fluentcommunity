# FluentCRM API — Reports

25 endpoints. Base URL: `https://{website}/wp-json/fluent-crm/v2`. See the [FluentCRM overview](../fluentcrm.md) for auth and the full group list.

_Generated from the FluentCRM OpenAPI specs (developers.fluentcrm.com)._

---

## DELETE `/reports/emails`

**DELETE Report Emails**

Delete campaign email records by their IDs. This permanently removes the email log entries from the database.

<!-- fc:access -->

**Required capability:** `fcrm_manage_email_delete`

_Enforced by `ReportPolicy::deleteEmails()`._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Request body** (`application/json`, required)

- `email_ids` (array<integer>) **required** — Array of campaign email IDs to delete.

Example:

```json
{
  "email_ids": [
    101,
    102,
    103
  ]
}
```


**Responses**

- **200** — Emails deleted successfully.

  Schema (`application/json`):

  - `message` (string)

  Example:

```json
{
  "message": "Selected emails have been deleted"
}
```



---

## GET `/reports/advanced-providers`

**GET Advanced Report Providers**

Retrieve the list of registered advanced report providers. Providers are registered via the `fluent_crm/advanced_report_providers` filter, typically by the Pro add-on or third-party integrations. Returns an empty array when no providers are registered.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Responses**

- **200** — Advanced report providers retrieved successfully.

  Schema (`application/json`):

  - `providers` (array<object>) — List of advanced report provider definitions.
    - _(object)_

  Example:

```json
{
  "providers": []
}
```



---

## GET `/reports/ajax-options`

**GET Ajax Options**

Retrieve searchable, paginated option items for various entity types. Used by select dropdowns that need server-side search. Supported `option_key` values include: `woo_categories`, `woo_products`, `product_selector_woo`, `product_selector_woo_order`, `edd_products`, `product_selector_edd`, `voxel_products`, `product_selector_voxel`, `voxel_product_types`, `campaigns`, `funnels`, `email_sequences`, `companies`, `post_type`, `company_industries`, `company_types`, `users`. Unrecognized keys are passed to the `fluentcrm_ajax_options_{key}` filter.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `option_key` | string | yes | The type of options to retrieve. |
| `search` | string | no | Search term to filter results. |
| `values` | array<integer> | no | Array of IDs that must be included in the results regardless of search term. |
| `sub_option_key` | string | no | Sub-type filter. Used with `woo_products` (for product type) and `post_type` (for the WordPress post type slug). |


**Responses**

- **200** — Options retrieved successfully.

  Schema (`application/json`):

  - `options` (array<object>)
    - `id` (any) — Option identifier.
    - `title` (string) — Display label.

  Example:

```json
{
  "options": [
    {
      "id": 5,
      "title": "Summer Campaign - 5"
    },
    {
      "id": 3,
      "title": "Welcome Series - 3"
    }
  ]
}
```



---

## GET `/reports/cascade_selections`

**GET Cascade Selections**

Retrieve cascading selection options for a given provider. The response is generated by the `fluent_crm/cascade_selection_options_{provider}` filter, allowing third-party integrations to supply hierarchical option data. Returns an empty options array by default if no filter is registered for the given provider.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider` | string | yes | Provider identifier that determines which filter supplies the cascade options. |


**Responses**

- **200** — Cascade selections retrieved successfully.

  Schema (`application/json`):

  - `options` (array<object>) — Hierarchical options provided by the registered filter.
    - _(object)_
  - `has_more` (boolean) — Whether additional options are available beyond the returned set.

  Example:

```json
{
  "options": [],
  "has_more": true
}
```



---

## GET `/reports/subscribers`

**GET Contact Growth**

Retrieve subscriber growth statistics over a date range. Returns a date-keyed object where each key is a date (or month label) and the value is the count of new subscribed contacts on that date. The frequency (daily, weekly, monthly) is automatically determined by the length of the date range.

The response is chart-ready: `data_sets` holds one series per range, and each series' `data` is a **map keyed by period label**, not an array. A second series is appended only when `compare_type` is set to something other than `no_comparison` and resolves to a valid window.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `date_range` | array<string> | no | Date range as a two-element array `[from, to]` in `YYYY-MM-DD` format. Defaults to the last 30 days if omitted. |
| `tag_id` | integer | no | Restrict growth to contacts carrying this tag. |
| `list_id` | integer | no | Restrict growth to contacts on this list. |
| `compare_type` | string | no | Adds a second comparison series. `custom` additionally requires `compare_range`; `no_comparison` and unrecognised values return a single series. |
| `compare_range[]` | array<string> | no | Explicit `[from, to]` comparison window. Used only when `compare_type` is `custom`, and ignored unless both entries are present. |


**Responses**

- **200** — Contact growth stats retrieved successfully.

  Schema (`application/json`):

  - `data_sets` (array<object>) — One entry for the current range, plus a second when `compare_type` requests a comparison.
    - `label` (string) — Series name — `Current Range` or `Compare Range`.
    - `data` (object) — Counts keyed by period label, e.g. `"Mar 2024": 262`. Bucket size is chosen from the range width.
      - _(object)_
    - `range` (array<string>) — The `[from, to]` window this series covers.
    - `backgroundColor` (string) — Chart fill colour.
    - `borderColor` (string) — Chart line colour.
    - `fill` (boolean) — Whether the chart area under the line is filled.
  - `current_range` (array<string>) — The resolved `[from, to]` window. Defaults to the last 30 days, derived from the SITE-LOCAL clock because `created_at` is written in site time.

  Example:

```json
{
  "data_sets": [
    {
      "label": "Current Range",
      "data": {
        "Mar 2024": 262,
        "Apr 2024": 143,
        "May 2024": 867
      },
      "range": [
        "2024-03-01",
        "2024-05-31"
      ],
      "backgroundColor": "#335CFF",
      "borderColor": "#335CFF",
      "fill": true
    }
  ],
  "current_range": [
    "2024-03-01",
    "2024-05-31"
  ]
}
```



---

## GET `/reports/dashboard-stats`

**GET Dashboard Stats**

Retrieve overall dashboard statistics including active contacts count, campaigns count, emails sent, active automations, onboarding progress, quick links, recent contacts, recent campaigns, active automations list, and system recommendations.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Responses**

- **200** — Dashboard statistics retrieved successfully.

  Schema (`application/json`):

  - `stats` (object) — Overall count statistics.
    - `total_subscribers` (StatItem)
    - `total_campaigns` (StatItem)
    - `email_sent` (StatItem)
    - `total_automations` (StatItem)
    - `email_pending` (any) — Present only when there are pending emails.
  - `sales` (array<object>) — Sales statistics (populated by Pro add-on via filter).
    - _(object)_
  - `dashboard_notices` (array<string>) — Admin notices for the dashboard (e.g., cron warnings).
  - `onboarding` (any) — Onboarding checklist progress. Null if onboarding is complete.
  - `quick_links` (array<object>) — Quick navigation links for the dashboard.
    - `title` (string)
    - `url` (string)
    - `icon` (string)
    - `is_external` (boolean)
  - `ff_config` (object) — Fluent Forms integration status.
    - `is_installed` (boolean)
    - `create_form_link` (string)
  - `recommendation` (any) — Pro upgrade recommendation based on installed plugins. False if Pro is active.
  - `system_tips` (any) — Database cleanup suggestion when email history exceeds 400,000 records. Empty string when not applicable.
  - `recent_contacts` (array<object>) — Most recent subscribed contacts (up to 3).
    - _(object)_
  - `active_automations` (array<object>) — Most recent published automations (up to 3).
    - _(object)_
  - `recent_campaigns` (array<object>) — Most recent archived campaigns with stats (up to 3).
    - _(object)_
  - `triggers` (array<object>) — Available funnel triggers (populated via filter).
    - _(object)_

  Example:

```json
{
  "stats": {
    "total_subscribers": {
      "title": "Active Contacts",
      "count": 1250,
      "route": {
        "name": "subscribers"
      }
    },
    "total_campaigns": {
      "title": "Campaigns",
      "count": 15,
      "route": {
        "name": "campaigns"
      }
    },
    "email_sent": {
      "title": "Emails Sent",
      "count": 45000,
      "route": {
        "name": "all_emails"
      }
    },
    "total_automations": {
      "title": "Active Automations",
      "count": 3,
      "route": {
        "name": "funnels"
      }
    }
  },
  "sales": [],
  "dashboard_notices": [],
  "onboarding": null,
  "quick_links": [
    {
      "title": "Contact Segments",
      "url": "/wp-admin/admin.php?page=fluentcrm-admin#/contact-groups/lists",
      "icon": "ContactSegments"
    }
  ],
  "ff_config": {
    "is_installed": true,
    "create_form_link": "/wp-admin/admin.php?page=fluent_forms#add=1"
  },
  "recommendation": false,
  "system_tips": "",
  "recent_contacts": [],
  "active_automations": [],
  "recent_campaigns": [],
  "triggers": []
}
```



---

## GET `/reports/email-clicks`

**GET Email Click Stats**

Retrieve email click statistics over a date range. Returns a date-keyed object where each key is a date (or month label) and the value is the count of link clicks tracked on that date. The frequency (daily, weekly, monthly) is automatically determined by the length of the date range.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `date_range` | array<string> | no | Date range as a two-element array `[from, to]` in `YYYY-MM-DD` format. Defaults to the last 30 days if omitted. |
| `campaign_id` | integer | no | Restrict the series to a single campaign. Omit or send 0 for site-wide totals. |


**Responses**

- **200** — Email click stats retrieved successfully.

  Schema (`application/json`):

  - `stats` (object) — Date-keyed object. Keys are `YYYY-MM-DD` (daily/weekly) or `Mon YYYY` (monthly). Values are click counts.
    - _(object)_

  Example:

```json
{
  "stats": {
    "2024-01-01": 25,
    "2024-01-02": 40,
    "2024-01-03": 30
  }
}
```



---

## GET `/reports/email-opens`

**GET Email Open Stats**

Retrieve email open statistics over a date range. Returns a date-keyed object where each key is a date (or month label) and the value is the count of emails opened on that date. The frequency (daily, weekly, monthly) is automatically determined by the length of the date range.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `date_range` | array<string> | no | Date range as a two-element array `[from, to]` in `YYYY-MM-DD` format. Defaults to the last 30 days if omitted. |
| `campaign_id` | integer | no | Restrict the series to a single campaign. Omit or send 0 for site-wide totals. |


**Responses**

- **200** — Email open stats retrieved successfully.

  Schema (`application/json`):

  - `stats` (object) — Date-keyed object. Keys are `YYYY-MM-DD` (daily/weekly) or `Mon YYYY` (monthly). Values are email open counts.
    - _(object)_

  Example:

```json
{
  "stats": {
    "2024-01-01": 80,
    "2024-01-02": 120,
    "2024-01-03": 95
  }
}
```



---

## GET `/reports/email-performance`

**GET Email Performance**

Retrieve aggregated email performance metrics including totals and percentages for sent, delivered, opened, clicked, and bounced emails. The `days` parameter controls the time window: a positive integer means the last N days, `0` means all time, and omitting it defaults to the last 30 days.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `days` | integer | no | Number of days to look back. Use `0` for all time. Defaults to 30 days if omitted. |


**Responses**

- **200** — Email performance metrics retrieved successfully.

  Schema (`application/json`):

  - `stats` (object)
    - `totals` (object) — Absolute counts for each metric.
      - `sent` (integer) — Total emails sent.
      - `delivered` (integer) — Delivered emails (sent minus bounced).
      - `opened` (integer) — Emails opened at least once.
      - `clicked` (integer) — Emails with at least one link click.
      - `bounced` (integer) — Bounced emails.
    - `percentages` (object) — Percentage rates. Delivery and bounce rates are based on (sent + bounced). Open and click rates are based on delivered.
      - `delivered` (number) — Delivery rate percentage.
      - `opened` (number) — Open rate percentage.
      - `clicked` (number) — Click rate percentage.
      - `bounced` (number) — Bounce rate percentage.

  Example:

```json
{
  "stats": {
    "totals": {
      "sent": 10000,
      "delivered": 9800,
      "opened": 4500,
      "clicked": 1200,
      "bounced": 200
    },
    "percentages": {
      "delivered": 96.08,
      "opened": 45.92,
      "clicked": 12.24,
      "bounced": 1.96
    }
  }
}
```



---

## GET `/reports/email-sents`

**GET Email Sent Stats**

Retrieve email sending statistics over a date range. Returns a date-keyed object where each key is a date (or month label) and the value is the count of emails sent on that date. The frequency (daily, weekly, monthly) is automatically determined by the length of the date range.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `date_range` | array<string> | no | Date range as a two-element array `[from, to]` in `YYYY-MM-DD` format. Defaults to the last 30 days if omitted. |
| `campaign_id` | integer | no | Restrict the series to a single campaign. Omit or send 0 for site-wide totals. |


**Responses**

- **200** — Email sent stats retrieved successfully.

  Schema (`application/json`):

  - `stats` (object) — Date-keyed object. Keys are `YYYY-MM-DD` (daily/weekly) or `Mon YYYY` (monthly). Values are sent email counts.
    - _(object)_

  Example:

```json
{
  "stats": {
    "2024-01-01": 150,
    "2024-01-02": 200,
    "2024-01-03": 175
  }
}
```



---

## GET `/reports/emails`

**GET Report Emails**

Retrieve a paginated list of campaign emails with their associated subscriber and campaign data. Optionally filter by email status. When requesting page 1, the response also includes a breakdown of email counts grouped by status.

<!-- fc:access -->

**Required capability:** `fcrm_read_emails`

_Enforced by `ReportPolicy::getEmails()`._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `status` | string | no | Filter emails by status (e.g., `sent`, `pending`, `failed`, `bounced`). |
| `page` | integer | no | Page number for pagination. When `1`, the response includes status counts. |
| `per_page` | integer | no | Number of emails per page. |
| `search` | string | no | Search across the recipient contact and the campaign. |
| `types[]` | array<string> | no | Filter by email type. Values are expanded server-side, so a broad type also matches its narrower variants. |


**Responses**

- **200** — Report emails retrieved successfully.

  Schema (`application/json`):

  - `emails` (object) — Paginated email results.
    - `current_page` (integer)
    - `data` (array<object>)
      - `id` (integer)
      - `campaign_id` (integer)
      - `subscriber_id` (integer)
      - `status` (string)
      - `email_subject` (string)
      - `email_address` (string)
      - `is_open` (integer) _(enum: `0`, `1`)_
      - `click_counter` (integer)
      - `scheduled_at` (string) _(format: date-time)_
      - `created_at` (string) _(format: date-time)_
      - `updated_at` (string) _(format: date-time)_
      - `subscriber` (object) — Associated contact data.
        - _(object)_
      - `campaign` (object) — Associated campaign data.
        - _(object)_
    - `last_page` (integer)
    - `per_page` (integer)
    - `total` (integer)
  - `statuses` (any) — Email counts grouped by status. Only returned on page 1, `null` otherwise.
  - `types` (array<object>) — Email types available as filter options.
    - _(object)_

  Example:

```json
{
  "emails": {
    "current_page": 1,
    "data": [
      {
        "id": 101,
        "campaign_id": 5,
        "subscriber_id": 42,
        "status": "sent",
        "email_subject": "Welcome!",
        "email_address": "john@example.com",
        "is_open": 1,
        "click_counter": 3,
        "scheduled_at": "2024-03-01 10:00:00",
        "created_at": "2024-03-01 09:55:00",
        "updated_at": "2024-03-01 12:30:00",
        "subscriber": {
          "id": 42,
          "email": "john@example.com"
        },
        "campaign": {
          "id": 5,
          "title": "Welcome Campaign"
        }
      }
    ],
    "last_page": 10,
    "per_page": 15,
    "total": 150
  },
  "statuses": {
    "sent": 120,
    "pending": 20,
    "failed": 5,
    "bounced": 5
  }
}
```



---

## GET `/reports/options`

**GET Report Options**

Retrieve option sets by specifying a comma-separated list of field names. Each field name maps to an internal method that returns the corresponding data (e.g., `countries`, `lists`, `tags`, `campaigns`, `statuses`, `editable_statuses`, `contact_types`, `email_sequences`, `automation_funnels`, `companies`, `segments`, `roles`, `user_roles_options`, `profile_sections`, `custom_fields`, `sms_statuses`). Only recognized field names are processed; unknown names are silently skipped.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `fields` | string | yes | Comma-separated list of option fields to retrieve (e.g., `lists,tags,statuses`). |
| `with_count` | array<string> | no | Array of field names for which to include subscriber counts. Currently supports `lists`. |


**Responses**

- **200** — Options retrieved successfully. The response contains an `options` object whose keys depend on the requested fields.

  Schema (`application/json`):

  - `options` (object) — Object containing the requested option sets. Keys match the requested field names.
    - `lists` (array<object>)
      - `id` (integer)
      - `slug` (string)
      - `title` (string)
      - `subscribersCount` (integer) — Only present when `with_count` includes `lists`.
    - `tags` (array<object>)
      - `id` (integer)
      - `slug` (string)
      - `title` (string)
      - `value` (string)
      - `label` (string)
    - `statuses` (array<object>)
      - _(object)_
    - `campaigns` (array<object>)
      - `id` (integer)
      - `title` (string)
    - `countries` (array<object>)
      - `code` (string)
      - `id` (string)
      - `slug` (string)
      - `title` (string)

  Example:

```json
{
  "options": {
    "lists": [
      {
        "id": 1,
        "slug": "newsletter",
        "title": "Newsletter"
      }
    ],
    "tags": [
      {
        "id": 1,
        "slug": "vip",
        "title": "VIP",
        "value": "1",
        "label": "VIP"
      }
    ],
    "statuses": [
      {
        "id": "subscribed",
        "title": "Subscribed"
      }
    ]
  }
}
```


- **422** — Missing `fields` parameter.

  Schema (`application/json`):

  - `message` (string)

  Example:

```json
{
  "message": "Missing requested fields field."
}
```



---

## GET `/reports/taxonomy-terms`

**GET Taxonomy Terms**

Retrieve WordPress taxonomy terms with optional search filtering. Returns up to 20 terms matching the search query, plus any additional terms specified by `values` IDs. Useful for populating dropdowns for category/tag-based filters.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `taxonomy` | string | yes | WordPress taxonomy slug (e.g., `category`, `post_tag`, `product_cat`). |
| `search` | string | no | Search term to filter taxonomy terms by name. |
| `values` | array<integer> | no | Array of term IDs that must be included in the results regardless of search. |


**Responses**

- **200** — Taxonomy terms retrieved successfully.

  Schema (`application/json`):

  - `options` (array<object>)
    - `id` (string) — Term ID as string.
    - `title` (string) — Term name.

  Example:

```json
{
  "options": [
    {
      "id": "15",
      "title": "Uncategorized"
    },
    {
      "id": "22",
      "title": "News"
    }
  ]
}
```



---

## GET `/reports/ping`

**GET Ping Report**

Simple health check endpoint that returns a `pong` message. Useful for verifying API connectivity and authentication.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Responses**

- **200** — Ping response.

  Schema (`application/json`):

  - `message` (string)

  Example:

```json
{
  "message": "pong"
}
```



---

## GET `/reports/automations`

**GET Automation Reports**

Paginated performance summary for every **published** automation, newest first. Drafts are excluded.

The four progression counts (`total_subscribers`, `completed_count`, `in_progress_count`, `last_run_at`) are produced by a single grouped aggregate across the whole page rather than per-row queries, so page size does not multiply query count.

::: warning
This endpoint's aggregate is built with `selectRaw()`, which bypasses the prefix-adding query grammar. It is one of the two seed cases in the plugin's test suite — see `tests/smoke/routes.manifest.php`.
:::

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `per_page` | integer | no | Automations per page. |
| `page` | integer | no | Page number. |


**Responses**

- **200** — Paginated automation reports.

  Schema (`application/json`):

  - `automations` (object) — Standard paginator over published automations.
    - `total` (integer) — Total number of matching rows.
    - `per_page` (integer) — Rows per page.
    - `current_page` (integer) — Current page number.
    - `last_page` (integer) — Number of the final page.
    - `from` (integer) — Index of the first row on this page.
    - `to` (integer) — Index of the last row on this page.
    - `next_page_url` (string,null) — URL of the next page, or null.
    - `prev_page_url` (string,null) — URL of the previous page, or null.
    - `data` (array<AutomationReport>)
  - `overview` (object)
    - `total` (integer) — Number of published automations.
    - `subscribers` (integer) — Distinct contact enrolments across every published automation.
    - `completed` (integer) — Enrolments that finished.
    - `in_progress` (integer) — Enrolments still running.
  - `top_automations` (array<object>) — The five most-triggered automations, cached for five minutes alongside `overview`.
    - `id` (string) — Funnel id, returned as a string.
    - `title` (string) — Automation name.
    - `trigger_name` (string) — Starting hook.
    - `trigger_count` (integer) — Total enrolments.

  Example:

```json
{
  "automations": {
    "current_page": 1,
    "per_page": 15,
    "total": 4,
    "last_page": 1,
    "from": 1,
    "to": 4,
    "next_page_url": null,
    "prev_page_url": null,
    "data": [
      {
        "id": 11,
        "type": "funnels",
        "title": "New Form Submission (Fluent Forms)",
        "trigger_name": "fluentform_submission_inserted",
        "status": "published",
        "conditions": {
          "run_only_one": "yes"
        },
        "settings": {
          "form_id": "1",
          "subscription_status": "subscribed"
        },
        "created_by": "1",
        "created_at": "2026-05-14 09:38:47",
        "updated_at": "2026-05-14 09:39:46",
        "total_subscribers": 1,
        "completed_count": 1,
        "in_progress_count": 0,
        "last_run_at": "2026-05-14 09:41:02",
        "recent_subscribers": []
      }
    ]
  }
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/automations/{id}/steps`

**GET Automation Step Report**

Funnel-shaped drop-off report for a single automation: how many contacts reached each step, and how many were lost between steps.

`stats` always opens with a synthetic **Entrance** row (`sequence_id: 0`, `type: "root"`) holding the distinct entrant count, which every later step is measured against. Steps that no contact has reached are omitted entirely, so `stats` is not a complete list of the automation's steps.

`percent` is clamped to 100 while `percent_text` keeps the uncapped value, so a step re-entered by the same contact can read above 100 in the label but not in the bar.

**`stats` is an empty array when the automation has no steps** — that is a valid response, not an error.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Path parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `id` | integer | yes | Funnel (automation) id. |


**Responses**

- **200** — The automation and its per-step funnel.

  Schema (`application/json`):

  - `funnel` (object)
    - `id` (integer) — Funnel id.
    - `type` (string) — Always `funnels`.
    - `title` (string) — Automation name.
    - `trigger_name` (string) — Starting hook.
    - `status` (string) _(enum: `draft`, `published`)_ — Automation status.
    - `conditions` (object)
      - _(object)_
    - `settings` (object)
      - _(object)_
    - `created_by` (string) — Author's WordPress user id.
    - `created_at` (string) _(format: date-time)_ — Creation timestamp.
    - `updated_at` (string) _(format: date-time)_ — Last update timestamp.
  - `stats` (array<object>) — One row per reached step, preceded by the Entrance row. Empty when the automation has no steps.
    - `label` (string) — Step name, or “Entrance” for the root row.
    - `count` (integer) — Contacts that reached this step.
    - `sequence_id` (integer) — Step id, or 0 for the Entrance row.
    - `type` (string) — Step type, or `root` for the Entrance row.
    - `percent` (integer) — Share of entrants that reached this step, clamped to 100.
    - `percent_text` (integer) — Same share, uncapped.
    - `previous_step_count` (integer) — Count at the preceding step.
    - `drop_count` (integer) — Contacts lost since the preceding step.
    - `drop_percent` (integer) — Percentage lost since the preceding step.

  Example:

```json
{
  "funnel": {
    "id": 1,
    "type": "funnels",
    "title": "Login and Add to Community",
    "trigger_name": "wp_login",
    "status": "draft",
    "conditions": {
      "run_multiple": "no"
    },
    "settings": {
      "subscription_status": "subscribed"
    },
    "created_by": "1",
    "created_at": "2024-03-27 00:54:06",
    "updated_at": "2024-09-27 21:21:24"
  },
  "stats": [
    {
      "label": "Entrance",
      "count": 120,
      "sequence_id": 0,
      "type": "root",
      "percent": 100,
      "percent_text": 100,
      "previous_step_count": 120,
      "drop_count": 0,
      "drop_percent": 0
    },
    {
      "label": "Send Welcome Email",
      "count": 96,
      "sequence_id": 42,
      "type": "action",
      "percent": 80,
      "percent_text": 80,
      "previous_step_count": 120,
      "drop_count": 24,
      "drop_percent": 20
    }
  ]
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_
- **404** — The requested resource does not exist.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/campaign-options`

**GET Campaign Options**

Minimal `{id, title}` list of **archived** (i.e. already sent) campaigns, newest updated first. Built to populate the campaign selector on the reports screens.

This is a plain `limit`, not a paginator: there is no `page` parameter and no total. Narrow the result with `search`, whose LIKE wildcards are escaped and matched literally.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `search` | string | no | Case-insensitive substring match on the campaign title. |
| `per_page` | integer | no | Maximum number of options to return. |


**Responses**

- **200** — Campaign options.

  Schema (`application/json`):

  - `options` (array<object>)
    - `id` (integer) — Campaign id.
    - `title` (string) — Campaign title.

  Example:

```json
{
  "options": [
    {
      "id": 10043,
      "title": "Spring release announcement"
    },
    {
      "id": 10002,
      "title": "March newsletter"
    }
  ]
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/campaigns-list`

**GET Campaigns List Report**

Paginated list of **archived** campaigns, most recently updated first, each with its `stats` block attached.

Rows are full campaign records — including `email_body` — so responses are large. When you only need identifiers for a picker, use `GET /reports/campaign-options` instead.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `per_page` | integer | no | Campaigns per page. |
| `page` | integer | no | Page number. |


**Responses**

- **200** — Paginated campaigns with stats.

  Schema (`application/json`):

  - `campaigns` (object) — Standard paginator over archived campaigns.
    - `total` (integer) — Total number of matching rows.
    - `per_page` (integer) — Rows per page.
    - `current_page` (integer) — Current page number.
    - `last_page` (integer) — Number of the final page.
    - `from` (integer) — Index of the first row on this page.
    - `to` (integer) — Index of the last row on this page.
    - `next_page_url` (string,null) — URL of the next page, or null.
    - `prev_page_url` (string,null) — URL of the previous page, or null.
    - `data` (array<object>)
      - `id` (integer) — Campaign id.
      - `title` (string) — Campaign title.
      - `status` (string) — Always `archived` here.
      - `email_subject` (string) — Subject line, smart codes unresolved.
      - `email_body` (string) — Full email body markup.
      - `updated_at` (string) _(format: date-time)_ — Last update timestamp.
      - `stats` (object)
        - `total` (integer) — Emails generated for this campaign.
        - `sent` (integer) — Emails sent.
        - `opened` (integer) — Emails opened.
        - `clicked` (integer) — Emails with at least one click.

  Example:

```json
{
  "campaigns": {
    "current_page": 1,
    "per_page": 15,
    "total": 48,
    "last_page": 4,
    "from": 1,
    "to": 15,
    "next_page_url": "/wp-json/fluent-crm/v2/reports/campaigns-list?page=2",
    "prev_page_url": null,
    "data": [
      {
        "id": 10043,
        "title": "Spring release announcement",
        "status": "archived",
        "email_subject": "{{contact.first_name|Hi}}, here is what shipped",
        "email_body": "<!-- wp:paragraph --><p>Hi there,</p><!-- /wp:paragraph -->",
        "updated_at": "2026-07-30 11:02:14",
        "stats": {
          "total": 628,
          "sent": 628,
          "opened": 532,
          "clicked": 418
        }
      }
    ]
  }
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/contacts-by-country`

**GET Contacts By Country**

Contact counts grouped by country, highest first.

Country values are normalised in SQL with `UPPER(TRIM(...))` before grouping, so casing and stray whitespace do not split a country across rows. Contacts with a null or blank country are excluded, which means the counts here will not add up to the site's total contact count.

Note that `country_code` reflects whatever is stored on the contact record — it is **not** validated against ISO 3166, so a site that stores full country names will see those instead of two-letter codes.

Takes no parameters and is not paginated.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Responses**

- **200** — Counts per country, descending.

  Schema (`application/json`):

  - `countries` (array<object>)
    - `country_code` (string) — Uppercased, trimmed country value as stored on the contact.
    - `contact_count` (integer) — Number of contacts.

  Example:

```json
{
  "countries": [
    {
      "country_code": "US",
      "contact_count": 4210
    },
    {
      "country_code": "BD",
      "contact_count": 883
    }
  ]
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/contacts-by-lists`

**GET Contacts By Lists**

Paginated contact counts per list, largest first. The list-side counterpart to `GET /reports/contacts-by-tags` and it behaves identically: empty lists are included with a count of 0, and `contact_count` is returned as a **string**.

The pivot join is constrained to `object_type = 'FluentCrm\App\Models\Lists'`, which is what keeps tag memberships out of these counts.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `per_page` | integer | no | Lists per page. |
| `page` | integer | no | Page number. |


**Responses**

- **200** — Paginated list counts.

  Schema (`application/json`):

  - `lists` (object) — Standard paginator over lists, ordered by contact count descending.
    - `total` (integer) — Total number of matching rows.
    - `per_page` (integer) — Rows per page.
    - `current_page` (integer) — Current page number.
    - `last_page` (integer) — Number of the final page.
    - `from` (integer) — Index of the first row on this page.
    - `to` (integer) — Index of the last row on this page.
    - `next_page_url` (string,null) — URL of the next page, or null.
    - `prev_page_url` (string,null) — URL of the previous page, or null.
    - `data` (array<object>)
      - `id` (integer) — List id.
      - `title` (string) — List name.
      - `contact_count` (string) — Number of contacts on the list, as a string.

  Example:

```json
{
  "lists": {
    "current_page": 1,
    "per_page": 20,
    "total": 2,
    "last_page": 1,
    "from": 1,
    "to": 2,
    "next_page_url": null,
    "prev_page_url": null,
    "data": [
      {
        "id": 1,
        "title": "Community Members",
        "contact_count": "11648"
      },
      {
        "id": 8,
        "title": "Product updates",
        "contact_count": "1"
      }
    ]
  }
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/contacts-by-status`

**GET Contacts By Status**

Contact counts grouped by subscription status.

Every known status is always present — statuses with no contacts are returned with a count of 0 rather than omitted — and they come back in a fixed display order: `subscribed`, `unsubscribed`, `pending`, `bounced`, `complained`, `spammed`, `transactional`. That makes the response safe to render directly without filling gaps.

`total` is the sum across all statuses, i.e. every contact on the site.

Takes no parameters.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Responses**

- **200** — Counts per status.

  Schema (`application/json`):

  - `stats` (array<object>) — One row per status, in fixed display order.
    - `status` (string) _(enum: `subscribed`, `unsubscribed`, `pending`, `bounced`, `complained`, `spammed`, `transactional`)_ — Contact status.
    - `count` (integer) — Number of contacts with that status. 0 when none.
  - `total` (integer) — Total contacts across every status.

  Example:

```json
{
  "stats": [
    {
      "status": "subscribed",
      "count": 11770
    },
    {
      "status": "unsubscribed",
      "count": 1096
    },
    {
      "status": "pending",
      "count": 1
    },
    {
      "status": "bounced",
      "count": 0
    },
    {
      "status": "complained",
      "count": 0
    },
    {
      "status": "spammed",
      "count": 0
    },
    {
      "status": "transactional",
      "count": 0
    }
  ],
  "total": 12867
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/contacts-by-tags`

**GET Contacts By Tags**

Paginated contact counts per tag, most-used first.

A LEFT JOIN is used, so tags with no contacts are included with a count of 0 rather than dropped.

`contact_count` is a SQL aggregate and comes back as a **string**, not a number.

The pivot join is constrained to `object_type = 'FluentCrm\App\Models\Tag'` — `fc_subscriber_pivot` is shared between tags and lists, so the discriminator is what keeps list memberships out of these counts.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `per_page` | integer | no | Tags per page. |
| `page` | integer | no | Page number. |


**Responses**

- **200** — Paginated tag counts.

  Schema (`application/json`):

  - `tags` (object) — Standard paginator over tags, ordered by contact count descending.
    - `total` (integer) — Total number of matching rows.
    - `per_page` (integer) — Rows per page.
    - `current_page` (integer) — Current page number.
    - `last_page` (integer) — Number of the final page.
    - `from` (integer) — Index of the first row on this page.
    - `to` (integer) — Index of the last row on this page.
    - `next_page_url` (string,null) — URL of the next page, or null.
    - `prev_page_url` (string,null) — URL of the previous page, or null.
    - `data` (array<object>)
      - `id` (integer) — Tag id.
      - `title` (string) — Tag name.
      - `contact_count` (string) — Number of contacts carrying the tag, as a string.

  Example:

```json
{
  "tags": {
    "current_page": 1,
    "per_page": 20,
    "total": 16,
    "last_page": 1,
    "from": 1,
    "to": 16,
    "next_page_url": null,
    "prev_page_url": null,
    "data": [
      {
        "id": 10,
        "title": "FluentCRM Members",
        "contact_count": "2347"
      },
      {
        "id": 16,
        "title": "FluentCart Purchased",
        "contact_count": "2028"
      }
    ]
  }
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/email-unsubs`

**GET Email Unsubscribe Stats**

Daily unsubscribe counts for a date range, ready to plot as a time series.

`stats` is a **map keyed by date** (`YYYY-MM-DD`), not an array. Every day in the range is present, including days with no unsubscribes, so the series has no gaps.

`date_range` is a two-element array: `date_range[0]` is the start and `date_range[1]` the end. Omitting it falls back to the reporting service's default window.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `date_range[]` | array<string> | no | Two-element range as `[from, to]`, each `YYYY-MM-DD`. Omit for the default window. |


**Responses**

- **200** — Unsubscribes per day.

  Schema (`application/json`):

  - `stats` (object) — Date-keyed counts. Every day in the range is present, zero-filled.
    - _(object)_

  Example:

```json
{
  "stats": {
    "2026-08-01": 3,
    "2026-08-02": 0,
    "2026-08-03": 1,
    "2026-08-04": 0
  }
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/recent-tags`

**GET Recently Created Tags**

The most recently **created** tags, each with its current contact count.

Ordering is by `created_at` descending — this is a “what's new” widget, not a popularity ranking. For that, use `GET /reports/contacts-by-tags`.

Not paginated: `per_page` is a plain limit and defaults to 5. `contact_count` comes back as a **string**.

<!-- fc:access -->

**Required capability:** `fcrm_view_dashboard`

_Enforced by `ReportPolicy::verifyRequest()`, the policy default for this route group._

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `per_page` | integer | no | Maximum number of tags to return. |


**Responses**

- **200** — Recently created tags.

  Schema (`application/json`):

  - `tags` (array<object>)
    - `id` (integer) — Tag id.
    - `title` (string) — Tag name.
    - `created_at` (string) _(format: date-time)_ — When the tag was created.
    - `contact_count` (string) — Number of contacts carrying the tag, as a string.

  Example:

```json
{
  "tags": [
    {
      "id": 16,
      "title": "FluentCart Purchased",
      "created_at": "2025-11-17 18:09:49",
      "contact_count": "2028"
    },
    {
      "id": 15,
      "title": "Manually Added",
      "created_at": "2025-10-07 20:40:58",
      "contact_count": "36"
    }
  ]
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---

## GET `/reports/top-campaigns`

**GET Top Campaigns**

<Badge type="warning" text="Pro" />

Best-performing sent campaigns, ranked by open rate, click rate, or volume.

Only rows where **all** of the following hold are counted: campaign status `archived`, campaign type `campaign`, and email status `sent`. Recurring campaigns, sequence emails, and automation emails are therefore excluded by design.

`open_rate` and `click_rate` are percentages computed in SQL and come back as **strings** with full decimal precision (for example `"84.7134"`), as does `total_sent`. Round them client-side.

An unrecognised `sort_by` silently falls back to `open_rate` rather than erroring.

<!-- fc:access -->

**Required capability:** `fcrm_manage_settings`

_Enforced by `SettingsPolicy::verifyRequest()`, the policy default for this route group._

**Requires:** FluentCampaign Pro. Without it the route does not exist.

<!-- /fc:access -->

**Auth:** ApplicationPasswords

**Query parameters**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `sort_by` | string | no | Ranking metric. Unrecognised values fall back to `open_rate`. |
| `per_page` | integer | no | Maximum number of campaigns to return. |


**Responses**

- **200** — Ranked campaigns.

  Schema (`application/json`):

  - `campaigns` (array<object>)
    - `id` (string) — Campaign id, returned as a string.
    - `title` (string) — Campaign title.
    - `updated_at` (string) _(format: date-time)_ — Last update timestamp.
    - `total_sent` (string) — Number of sent emails, as a string.
    - `open_rate` (string) — Open rate percentage with full decimal precision.
    - `click_rate` (string) — Click rate percentage with full decimal precision.
  - `available` (boolean) — Always true — present so the dashboard can tell a Pro-enabled response from a Free stub.

  Example:

```json
{
  "campaigns": [
    {
      "id": "17",
      "title": "Join the private group",
      "updated_at": "2024-11-19 18:23:58",
      "total_sent": "628",
      "open_rate": "84.7134",
      "click_rate": "66.5605"
    },
    {
      "id": "3",
      "title": "0.61 release notes",
      "updated_at": "2024-04-03 22:56:10",
      "total_sent": "318",
      "open_rate": "76.4151",
      "click_rate": "15.4088"
    }
  ],
  "available": true
}
```


- **401** — Not authenticated — missing or invalid credentials.

  Schema (`application/json`):

  - _$ref: Error_
- **403** — Authenticated but the user lacks the capability this route requires.

  Schema (`application/json`):

  - _$ref: Error_

---
